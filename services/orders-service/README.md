# orders-service

Microservicio encargado de exponer operaciones básicas de órdenes.

## Endpoints

- `GET /health`
- `GET /orders`
- `GET /orders/{order_id}`
- `POST /orders`

## Desarrollo local

Instalar dependencias:

```bash
pip install -r requirements.txt