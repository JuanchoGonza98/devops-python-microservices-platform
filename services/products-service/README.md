# products-service

Microservicio encargado de exponer el catálogo de productos.

## Endpoints

- `GET /health`
- `GET /products`
- `GET /products/{product_id}`

## Desarrollo local

Instalar dependencias:

```bash
pip install -r requirements.txt