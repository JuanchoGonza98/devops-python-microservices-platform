# users-service

Microservicio encargado de exponer operaciones básicas de usuarios.

## Endpoints

- `GET /health`
- `GET /users`
- `GET /users/{user_id}`
- `POST /users`

## Desarrollo local

Instalar dependencias:

```bash
pip install -r requirements.txt