resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: orders-service-config
    literals:
      - APP_ENV=production
      - LOG_LEVEL=INFO
      - SERVICE_NAME=orders-service