apiVersion: v1
kind: Service
metadata:
  name: products-service
  labels:
    app: products-service
    app.kubernetes.io/name: products-service
spec:
  type: ClusterIP
  selector:
    app: products-service
  ports:
    - name: http
      protocol: TCP
      port: 8001
      targetPort: 8001