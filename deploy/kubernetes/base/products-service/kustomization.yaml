resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: products-service-config
    literals:
      - APP_ENV=production
      - LOG_LEVEL=INFO
      - SERVICE_NAME=products-service