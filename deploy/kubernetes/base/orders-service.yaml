apiVersion: v1
kind: Service
metadata:
  name: orders-service
  labels:
    app: orders-service
    app.kubernetes.io/name: orders-service
spec:
  type: ClusterIP
  selector:
    app: orders-service
  ports:
    - name: http
      protocol: TCP
      port: 8004
      targetPort: 8004