apiVersion: v1
kind: Service
metadata:
  name: users-service
  labels:
    app: users-service
    app.kubernetes.io/name: users-service
spec:
  type: ClusterIP
  selector:
    app: users-service
  ports:
    - name: http
      protocol: TCP
      port: 8002
      targetPort: 8002