apiVersion: v1
kind: Service
metadata:
  name: payments-service
  labels:
    app: payments-service
    app.kubernetes.io/name: payments-service
spec:
  type: ClusterIP
  selector:
    app: payments-service
  ports:
    - name: http
      protocol: TCP
      port: 8003
      targetPort: 8003